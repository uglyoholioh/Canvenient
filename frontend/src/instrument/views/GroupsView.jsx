// Groups — demoted to a utility, still fully functional (existing view).

import GroupsViewCore from "../../components/GroupsView";

export default function GroupsView(props) {
  return <GroupsViewCore {...props} />;
}
